#include <stdio.h>
#include "eval.h"
#include "play.h"
#include "lmath.h"
#include "uci.h"


int main()
{
    // MoveList* list = {0};
    // legalMoveGen(list);
    uciStart();
    // makeMove(board,0);// 0 white black 1
    printf("\n");

    return 1;
}
